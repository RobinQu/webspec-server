<table class="table">
  <thead>
    <tr>
      <th></th>
      <% _.forEach(browsers, function(value) { %>
        <th><%= value %></th>
      <% }) %>
    </tr>
  </thead>
  <tbody>
    <% _.forEach(rows, function(row, i) { %>
    <tr>
      <th><%= commits[i] %></th>
      <td>
        <span class="label label-info"><%= row.all %> Runs</span>
        <span class="label label-danger"><%= row.ok %> Passed</span>
      </td>
    </tr>
    <% }) %>
  </tbody>
</table>