<!DOCTYPE html>
<html>
  <head>
    <title>Repo <%= repo %> - Spec - WebSpec</title>
  </head>
  <body>
    <div class="article">
      Running spec files in repo (<%= repo %>): 
      <ul>
      <% specs.forEach(function(spec) { %>
        <li><%= spec %></li>
      <% }) %>
      </ul>
    </div>
  <iframe src="/sandboxes/<%= uuid %>?repo=<%= repo %>" frameborder="0"></iframe>
  </body>
</html>